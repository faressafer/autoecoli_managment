// Client-side Firebase exports
export { auth, db, storage } from './config';

// Server-side Firebase Admin exports (only use in API routes or server components)
export { adminAuth, adminDb } from './admin';

